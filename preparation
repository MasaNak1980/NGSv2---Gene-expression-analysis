# Python3.7 install
# Anacondaをインストール（Anacondaを使った経験があったので、minicodaではなく、anacondaを選択
# ここからインストール
  https://www.anaconda.com/products/individual
  
# terminal で 以下を追加

　conda config --add channels defaults
  conda config --add channels conda-forge
  conda config --add channels bioconda

# I tried to install sra-tools using following command, but, 

conda install -c bioconda sra-tools

# didn't work

conda install -c bioconda/label/cf201901 sra-tools 

# success! 

# making a directory at a USB connected HDD

mkdir /Volumes/xxxxxx/expression/seq 

cd /Volumes/xxxxxx/expression/seq

# Runinfo Table と Accession List を上記ディレクトリにダウンロード
fasterq-dumpを用いてRNA-seqデータをダウンロード（2020/02/12 タイポがありました。Twitterにてご報告くださった方ありがとうございました。）
cat SRR_Acc_List.txt | while read line; do cmd="fasterq-dupm --split-files ${line}; gzip ${line}*fastq"; eval ${cmd}; done
cat SRR_Acc_List.txt | while read line; do cmd="fasterq-dump --split-files ${line}; gzip ${line}*fastq"; eval ${cmd}; done
ダウンロードしたファイルを一覧
ls -lh *fastq.gz

